<!-- HEADER -->
<%-include ../partial/header %>
<%-include ../partial/header-nav-2 %>
<style>
    #unavailableDatesInfo ul li{
        list-style-type: disc;
        margin: 0px 0px 0px 20px;
        padding: 0px;
    }

    #unavailableDatesInfo{
        font-size: 12px;
        padding-left: 20px;
    }
</style>

<!-- DESTINATION BANNER -->
<div class="enquiry-banner have-curtain mb-0 backgroundImage">
    <div class="enquiry-banner--inner">
        <div class="container">
            <div class="destination-banner--text">
                <div class="welcome-title--text mb-3">
                    <h1 class="resortName"></h1>
                    <div class="d-block mb-4">
                        <p class="mb-0"><i class="fas fa-map fa-sm mr-2"></i><span class="resortLocation"></span></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="section section-xs bg-light">
    <div class="container">
        <div class="btn-group mb-4 btn-group-resort">
            <a class="btn btn-lg btn-primary">1. Choose Date</a>
            <a href="checkout-resort-2" class="btn btn-lg btn-outline-primary disabled">2. Add Extras</a>
            <a href="checkout-resort-3" class="btn btn-lg btn-outline-primary disabled">3. Guest Detail</a>
            <a href="checkout-resort-4" class="btn btn-lg btn-outline-primary disabled">4. Reserve</a>
        </div>
        <div class="card rounded-0 py-2 mb-5">
            <div class="card-body">
                <div class="row">
                    <div class="col-md-8">
                        <div class="mb-3">
                            <div class="d-flex align-items-center justify-content-between">
                                <h5 class="panel-title d-block">Unavailable Dates</h5>
                            </div>
                            <div id="unavailableDatesInfo">
                                
                            </div>
                        </div>
                        <div id="roomDate" style="display: none !important;" class="d-block mb-5 border-bottom pb-5">
                            <div class="d-flex align-items-center justify-content-between mb-4">
                                <h5 class="panel-title d-block">Choose Room Date</h5>
                            </div>
                            <div class="d-block p-3 bg-lighter border">
                                <div class="row">
                                    <div class="col-md-6">
                                        <label for="checkin_date">Select check in date</label>
                                        <input id="roomDateFrom" type="text" class="form-control datePicker" required>
                                    </div>
                                    <div class="col-md-6">
                                        <label for="checkin_date">Select check out date</label>
                                        <input id="roomDateTo" type="text" class="form-control datePicker" required>
                                    </div>
                                </div>
                            </div> 
                        </div>
                        <div id="packageDate" style="display: none !important;" class="d-block mb-4">
                            <div class="d-flex align-items-center justify-content-between mb-4">
                                <h5 class="panel-title d-block">Choose Trip Date</h5>
                            </div>
                        </div>
                        
                        <div class="d-flex justify-content-end w-100">
                            <button type="button" onClick="checkout()" class="hide-mobile btn btn btn-outline-primary">Continue to Step 2</button>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="checkout-summary">
                            <div id="checkoutCover" class="checkout-summary-cover">
                                <div class="d-flex h-100 justify-content-center align-items-center">
                                    <div class="text-center">
                                        <div class="spinner-border mb-3 text-dark" role="status">
                                            <span class="sr-only">Loading...</span>
                                        </div>
                                        <div class="text-muted">Please wait...</div>
                                    </div>
                                </div>
                            </div>
                            <div class="checkout-summary--head">
                                <div class="checkout-summary--img backgroundImage"></div>
                                <div class="checkout-summary--title">
                                    <h4 class="resortName"></h4>
                                    <div class="d-block">
                                        <p class="mb-0"><i class="fas fa-map fa-sm mr-2"></i><span class="resortLocation"></span></p>
                                    </div>
                                </div>
                            </div>
                            <div class="checkout-summary--list">
                                <ul class="list-group list-group-flush">
                                    <div id="listRoom"></div>
                                    <div id="listPackage"></div>
                                    <!-- <li class="list-group-item">
                                        <div class="list-person">
                                            <div class="list-person--title">
                                                <div>Twin Bungalow [Room 1]</div>
                                                <div>$240</div>
                                            </div>
                                            <div class="list-person--item text-muted">
                                                <div><i class="far fa-moon fa-sm fas-fix mr-1"></i>$120 x2</div>
                                                <div>$240</div>
                                            </div>
                                            <div class="list-person--item text-muted">
                                                <div><i class="far fa-user fa-sm fas-fix mr-1"></i>x2</div>
                                            </div>
                                        </div>
                                    </li>
                                    <li class="list-group-item">
                                        <div class="list-person">
                                            <div class="list-person--title">
                                                <div>Twin Bungalow [Room 2]</div>
                                                <div>$240</div>
                                            </div>
                                            <div class="list-person--item text-muted">
                                                <div><i class="far fa-moon fa-sm fas-fix mr-1"></i>$120 x2</div>
                                                <div>$240</div>
                                            </div>
                                            <div class="list-person--item text-muted">
                                                <div><i class="far fa-user fa-sm fas-fix mr-1"></i>x1</div>
                                            </div>
                                        </div>
                                    </li> -->
                                    <!-- <li class="list-group-item">
                                        <div class="list-person">
                                            <div class="list-person--title">
                                                <div>Komodo Weekender</div>
                                                <div>$2310</div>
                                            </div>
                                            <div class="list-person--item text-muted">
                                                <div><i class="far fa-user fa-sm fas-fix mr-1"></i>$770 x3</div>
                                                <div>$2310</div>
                                            </div>
                                        </div>
                                    </li> -->
                                    </ul>
                                    <div class="list-total">
                                    <!-- <div class="list-total-item">
                                        <div>VAT 10%</div>
                                        <div id="totalResorPrice">$279</div>
                                    </div> -->
                                    <div class="list-total-item">
                                        <div>Total Price</div>
                                        <div id="totalResorPrice">IDR 0</div>
                                    </div>
                                </div> 
                            </div>
                        </div>
                    </div>
                </div>

            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col view-mobile-only mt-3">
                        <button type="button" onClick="checkout()" class="btn btn-block btn btn-outline-primary">Continue to Step 2</button>
                    </div>
                </div>
            </div>
            
    
        </div>
    </div>
</div>

<section id="overlay-checkout" class="d-none fixed-top vw-100 vh-100" style="background:rgba(0,0,0,0.4)">
    <div class="text-center d-table h-100 w-100">
        <div class="d-table-cell h-100 align-middle">
            <div class="spinner-border text-light" style="width: 2rem; height: 2rem;" role="status">
            </div>
            <span class='text-light'>loading. . .</span>
        </div>
    </div>
</section>


<!-- FOOTER -->
<%-include ../partial/footer-nav %>
<%-include ../partial/footer %>

<script src="/js/bootstrap-datepicker.min.js"></script>
<script>

    var id = "<%= id %>"

    var package_data

    var totalPriceData
    var totalPrice = 0

    var someDate = new Date();
    var numberOfDaysToAdd = 3;
    someDate.setDate(someDate.getDate() + numberOfDaysToAdd); 
    var unDates = []
    var unDatesAnnualStart = []
    var unDatesAnnualEnd = []
    var unDatesOnceStart = []
    var unDatesOnceEnd = []

    $(document).ready(function(){
        loadTransactionById()

        $('#roomDateFrom').datepicker({
            // format: "yyyy-mm-dd",
            format: "dd MM yyyy",
            autoclose: true,
        });

        $('#roomDateFrom').change(function() {

            //$('#roomDateTo').datepicker()
            someDate = new Date($('#roomDateFrom').val())
            someDate.setDate(someDate.getDate() + 1)
            console.log(someDate)

            $('#roomDateTo').datepicker('destroy').datepicker({
                // format: "yyyy-mm-dd",
                format: "dd MM yyyy",
                autoclose: true,
                todayHighlight: true,
                startDate: someDate
            });

            calculateDate()
        })

        $('#roomDateTo').change(function(){
            calculateDate()
        });

        $(document).on("focus", ".packageDateList", function(){
            let tgl = $(this).val();
            console.log(tgl)
            if (tgl == '') {
                $(this).datepicker({
                    // format: "yyyy-mm-dd",
                    format: "dd MM yyyy",
                    todayHighlight: true,
                    autoclose: true,
                    startDate: '+1d'
                });
            }else{
                // $(this).datepicker('setDate',tgl);
                $(this).datepicker({
                    // format: "yyyy-mm-dd",
                    format: "dd MM yyyy",
                    todayHighlight: true,
                    autoclose: true,
                    setDate: tgl
                });
            } 
        });
    })

    function calculateDate(){
        totalPrice = 0;
        if($('#roomDateFrom').val() == "" || $('#roomDateTo').val() == ""){
            return
        }

        if(new Date($('#roomDateFrom').val()) > new Date($('#roomDateTo').val())){
            // alert("Invalid date")
            return Swal.fire(
                'Oops!',
                'Be sure to choose the correct date.'
            )
            $('#roomDateTo').val("")
            $('#roomDateFrom').val("")
            // return
        }

        let tStart = dateFormatForDB($('#roomDateFrom').val())
        let tEnd = dateFormatForDB($('#roomDateTo').val())

        let tglStart = new Date(tStart)
        let tglEnd = new Date(tEnd)
        var night = (tglEnd - tglStart) / (1000 * 3600 * 24)

        
        if(package_data.room){
            checkUnavailableDates()
            
            var dataRoom = package_data.room
            for(var i = 0; i < dataRoom.length; i++){
                for(var j = 0; j < dataRoom[i].roomItem.length; j++){
                    $(`#night${dataRoom[i].roomItem[j].id}`).text(night)
                    var summaryPrice = 0
                    if(dataRoom[i].sharingRoom == "yes"){
                        summaryPrice = parseFloat(night) * parseFloat(dataRoom[i].roomPrice) * parseFloat(dataRoom[i].roomItem[j].guest)
                    }else{
                        summaryPrice = parseFloat(night) * parseFloat(dataRoom[i].roomPrice)
                    }
                    $(`#${dataRoom[i].roomItem[j].id}`).text(`IDR ${formatRupiah(summaryPrice)}`)
                    $(`#total${dataRoom[i].roomItem[j].id}`).text(formatRupiah(summaryPrice))
                    totalPrice += summaryPrice
                }
            }
        }

        if(package_data.package){
            var dataPackage = package_data.package
            for(var i = 0; i < dataPackage.length; i++){
                for(var j = 0; j < dataPackage[i].packageItem.length; j++){
                    //$(`#night${dataRoom[i].roomItem[j].id}`).text(night)
                    var summaryPrice = parseFloat(dataPackage[i].packageItem[j].guest) * parseFloat(dataPackage[i].packagePrice)
                    //$(`#${dataRoom[i].roomItem[j].id}`).text(formatRupiah(summaryPrice))
                    //$(`#total${dataRoom[i].roomItem[j].id}`).text(formatRupiah(summaryPrice))
                    totalPrice += summaryPrice
                }
            }
        }

        $('#totalResorPrice').text(`IDR ${formatRupiah(parseFloat(totalPrice))}`)
    }

    function checkUnavailableDates(){
        let tStart = dateFormatForDB($('#roomDateFrom').val())
        let tEnd = dateFormatForDB($('#roomDateTo').val())

        let tglStart = new Date(tStart)
        let tglEnd = new Date(tEnd)

        let y1 = tglStart.getFullYear();
        let y2 = tglEnd.getFullYear();

        var night = (tglEnd - tglStart) / (1000 * 3600 * 24)
        for (let m = 0; m < unDatesAnnualStart.length; m++) {
            let udStart = unDatesAnnualStart[m]
            let udEnd = unDatesAnnualEnd[m]

            let tS = new Date(udStart)
            let tE = new Date(udEnd)

            let diff = tE.getTime() - tS.getTime()
            let dayDiff = Math.ceil(diff/(1000*3600*24))

            let udStartGet = y1+'-'+udStart.toString().substr(5);
            let udEndGet = y1+'-'+udEnd.toString().substr(5);
            
            let tSNow = new Date(udStartGet)
            let tENow = new Date(udEndGet)

            // tENow.setDate(tENow.getDate()+dayDiff)
            // let mo = tENow.getMonth()+1;
            // mo = mo.toString().padStart(2,'0');
            // let udEndGet = tENow.getFullYear()+'-'+mo+'-'+tENow.getDate();

            // console.log(`${t1} >= ${tglStart}`,t1 >= tglStart);
            // console.log(`${t2} <= ${tglEnd}`,t2 <= tglEnd);
            // console.log(t2 > tglEnd);
            let text = '';
            if (udStart.toString() == udEnd.toString()) {
                text = dateFormatMonth(udStart)
            }else{
                text = `${dateFormatMonth(udStart)} - ${dateFormatMonth(udEnd)} `;
            }
            if (tSNow >= tglStart && tSNow <= tglEnd) {
                Swal.fire({
                    title: 'We Apologize',
                    html: `Resort <span style='color:red'>unavailable</span> on ${text}`
                })
                return false
            }else if (tENow >= tglStart && tENow <= tglEnd) {
                Swal.fire({
                    title: 'We Apologize',
                    html: `Resort <span style='color:red'>unavailable</span> on ${text}`
                })
                return false
            }
        }

        for (let m = 0; m < unDatesOnceStart.length; m++) {
            let udStart = unDatesOnceStart[m]
            let udEnd = unDatesOnceEnd[m]

            let tS = new Date(udStart)
            let tE = new Date(udEnd)

            let text = '';
            if (udStart.toString() == udEnd.toString()) {
                text = dateFormatMonth(udStart)
            }else{
                text = `${dateFormatMonth(udStart)} - ${dateFormatMonth(udEnd)} `;
            }
            
            if (tS >= tglStart && tS <= tglEnd) {
                Swal.fire({
                    title: 'We Apologize',
                    html: `Resort <span style='color:red'>unavailable</span> on ${text} `
                })
                return false
            }else if (tE >= tglStart && tE <= tglEnd) {
                Swal.fire({
                    title: 'We Apologize',
                    html: `Resort <span style='color:red'>unavailable</span> on ${text} `
                })
                return false
            }
        }

        return true
    }
    
    function checkUnavailableDatesPackage(inputId){
        let tgl = dateFormatForDB($(`#${inputId}`).val())
        let tglUmum = new Date(tgl)
        
        let y1 = tglUmum.getFullYear();

        for (let m = 0; m < unDatesAnnualStart.length; m++) {

            let udStart = unDatesAnnualStart[m]
            let udEnd = unDatesAnnualEnd[m]

            let tS = new Date(udStart)
            let tE = new Date(udEnd)

            let diff = tE.getTime() - tS.getTime()
            let dayDiff = Math.ceil(diff/(1000*3600*24))
            console.log('diff',dayDiff)

            let udStartGet = y1+'-'+udStart.toString().substr(5);
            let udEndGet = y1+'-'+udEnd.toString().substr(5);
            
            let tSNow = new Date(udStartGet)
            let tENow = new Date(udEndGet)
            
            let text = '';
            if (udStart.toString() == udEnd.toString()) {
                text = dateFormatMonth(udStart)
            }else{
                text = `${dateFormatMonth(udStart)} - ${dateFormatMonth(udEnd)} `;
            }

            if (tSNow >= tglUmum && tSNow <= tglUmum) {
                Swal.fire({
                    title: 'We Apologize',
                    html: `Resort <span style='color:red'>unavailable</span> on ${text}`
                })
                return false
            }else if (tENow >= tglUmum && tENow <= tglUmum) {
                Swal.fire({
                    title: 'We Apologize',
                    html: `Resort <span style='color:red'>unavailable</span> on ${text}`
                })
                return false
            }
        }

        for (let m = 0; m < unDatesOnceStart.length; m++) {
            let udStart = unDatesOnceStart[m]
            let udEnd = unDatesOnceEnd[m]

            let tS = new Date(udStart)
            let tE = new Date(udEnd)

            // console.log(`${t1} >= ${tglStart}`,t1 >= tglStart);
            // console.log(`${t1} <= ${tglEnd}`,t1 <= tglEnd);

            let text = '';
            if (udStart.toString() == udEnd.toString()) {
                text = dateFormatMonth(udStart)
            }else{
                text = `${dateFormatMonth(udStart)} - ${dateFormatMonth(udEnd)} `;
            }
            if (tS <= tglUmum && tE >= tglUmum) {
                Swal.fire({
                    title: 'We Apologize',
                    html: `Resort <span style='color:red'>unavailable</span> on ${text}`
                })
                return false
            }
        }

        return true
    }

    function checkAllPackageDates(){
        var result = true;
        $(".packageDateList").each(function(){
            let inputId = $(this).attr('id')
            let res = checkUnavailableDatesPackage(inputId)
            if (!res) {
                result = false
            }
        })

        return result;
    }
    

    function loadTransactionById(){
        $.ajax({
            url: `/api/transaction-resort/${id}`,
            method: `GET`,
            success: function (result) {
                console.log(result)
                if (result.success) {
                    $('#checkoutCover').addClass('hideImportant');
                    $('.backgroundImage').css("background-image", `url('${result.data.resort.photos[0].url}')`)
                    $('.resortName').text(result.data.resort.profile.name)
                    $('.resortLocation').text(`${result.data.resort.profile.sub_destination.name} - ${result.data.resort.profile.destination.name}, ${result.data.resort.profile.country.name}`)
                    package_data = result.data.package_data
                    
                    unDates = result.data.resort.unavailable_dates
                    
                    if (result.data.transaction_resort_status_id == 'b648ff73-468f-45cb-bbbc-fd3659e4b8ac') {
                        window.location.replace(`/checkout-resort/${result.data.id}/reserve`);
                    }
                    if(result.data.package_data.room){
                        let tgl_room = result.data.package_data.room_date;
                        if (typeof tgl_room !== 'undefined') {
                            // $('#roomDateFrom').val(dateFormatMonth(tgl_room.from));
                            // $('#roomDateTo').val(dateFormatMonth(tgl_room.to));

                            $('#roomDateFrom').datepicker('setDate',dateFormatMonth(tgl_room.from));
                            $('#roomDateTo').datepicker('setDate',dateFormatMonth(tgl_room.to));
                        }

                        $('#roomDate').show()
                        $('#checkout1Loader').addClass('hideImportant')
                        var dataRoom = result.data.package_data.room
                        for(var i = 0; i < dataRoom.length; i++){
                            for(var j = 0; j < dataRoom[i].roomItem.length; j++){

                                if(dataRoom[i].sharingRoom == "yes"){
                                    $('#listRoom').append(`
                                        <li id="${dataRoom[i].id}" class="list-group-item">
                                            <div class="list-person">
                                                <div class="list-person--title">
                                                    <div>${dataRoom[i].roomName} [Dorm ${j+1}]</div>
                                                    <div style="min-width:120px; font-weight:500; position:relative" class='text-right'>
                                                        <div style='position:absolute;right:0px; bottom:0px' id="${dataRoom[i].roomItem[j].id}">IDR ${formatRupiah(dataRoom[i].roomPrice * dataRoom[i].roomItem[j].guest)}</div>
                                                    </div>
                                                </div>
                                                <div class="list-person--item text-muted">
                                                    <div><i class="far fa-moon fa-sm fas-fix mr-1"></i>IDR ${formatRupiah(dataRoom[i].roomPrice * dataRoom[i].roomItem[j].guest)} x<span id="night${dataRoom[i].roomItem[j].id}">0</span></div>
                                                    <div>IDR <span id="total${dataRoom[i].roomItem[j].id}">0</span></div>
                                                </div>
                                                <div class="list-person--item text-muted">
                                                    <div><i class="far fa-user fa-sm fas-fix mr-1"></i>x${formatRupiah(dataRoom[i].roomItem[j].guest)}</div>
                                                </div>
                                            </div>
                                        </li>
                                    `)
                                }else{
                                    $('#listRoom').append(`
                                        <li id="${dataRoom[i].id}" class="list-group-item">
                                            <div class="list-person">
                                                <div class="list-person--title">
                                                    <div>${dataRoom[i].roomName} [Room ${j+1}]</div>
                                                    <div style="min-width:120px; font-weight:500; position:relative" class='text-right'>
                                                        <div style='position:absolute;right:0px; bottom:0px' id="${dataRoom[i].roomItem[j].id}">IDR ${formatRupiah(dataRoom[i].roomPrice)}</div>
                                                    </div>
                                                </div>
                                                <div class="list-person--item text-muted">
                                                    <div><i class="far fa-moon fa-sm fas-fix mr-1"></i>IDR ${formatRupiah(dataRoom[i].roomPrice)} x<span id="night${dataRoom[i].roomItem[j].id}">0</span></div>
                                                    <div>IDR <span id="total${dataRoom[i].roomItem[j].id}">0</span></div>
                                                </div>
                                                <div class="list-person--item text-muted">
                                                    <div><i class="far fa-user fa-sm fas-fix mr-1"></i>x${formatRupiah(dataRoom[i].roomItem[j].guest)}</div>
                                                </div>
                                            </div>
                                        </li>
                                    `)
                                }
                            }
                        }
                        
                        calculateDate()
                    }

                    if(result.data.package_data.package){
                        $('#packageDate').show()
                        var packageDetail = result.data.package_data.package


                        var htmlPackageDate = ``
                        for(var i = 0; i < packageDetail.length; i++){

                            var itemPackage = packageDetail[i].packageItem
                            var htmlItemPackage = ``

                            var pService = [];
                            if (typeof packageItem !== 'undefined' && typeof packageItem[x].service !== 'undefined' ) {
                                for (let p_idx = 0; p_idx < packageItem[x].service.length; p_idx++) {
                                    pService[`${packageItem[x].service[p_idx].id}`] = packageItem[x].service[p_idx].qty; 
                                }
                                
                            }

                            for(var j = 0; j < itemPackage.length; j++){
                                htmlItemPackage += `
                                    <div class="col-md-12">
                                        <label for="checkin_date">
                                            Day ${j + 1}<br/>
                                            ${itemPackage[j].guest} guest<br/>
                                            Select date
                                        </label>
                                        <input onChange="addPackageDate('${packageDetail[i].id}','${itemPackage[j].id}')" id="packageDateFrom-${packageDetail[i].id}-${itemPackage[j].id}" type="text" class="form-control datePicker packageDateList" value="${(typeof itemPackage[j].package_date !== 'undefined' ? dateFormatMonth(itemPackage[j].package_date) : '')}" required>
                                    </div>`

                                $('#listPackage').append(`
                                    <li class="list-group-item">
                                        <div class="list-person">
                                            <div class="list-person--title">
                                                <div>${packageDetail[i].packageName} [Schedule - ${j+1}]</div>
                                                <div style="min-width:120px; font-weight:500; position:relative" class='text-right'>
                                                    <div  style='position:absolute;right:0px; bottom:0px' >IDR ${formatRupiah(packageDetail[i].packagePrice)}</div>
                                                </div>
                                            </div>
                                            <div class="list-person--item text-muted">
                                                <div><i class="far fa-user fa-sm fas-fix mr-1"></i>IDR ${formatRupiah(parseFloat(packageDetail[i].packagePrice))} x${packageDetail[i].packageItem[j].guest}</div>
                                                <div>IDR ${formatRupiah(parseFloat(packageDetail[i].packagePrice)*parseFloat(packageDetail[i].packageItem[j].guest))} </div>
                                            </div>
                                        </div>
                                    </li> 
                                `)

                                totalPrice += parseFloat(packageDetail[i].packagePrice)*parseFloat(packageDetail[i].packageItem[j].guest)
                            }

                            $('#packageDate').append(`
                                <div class="d-block p-3 bg-lighter border">
                                    <div class="row">
                                        <div class="col-md-6">
                                            
                                            <div class="text-muted d-none">Package ${i+1}</div>
                                            <h5 class="m-0">${packageDetail[i].packageName}</h5>
                                        </div><br><br>
                                        ${htmlItemPackage}
                                        <!-- div class="col-md-6">
                                            <label for="checkin_date">Select date</label>
                                            <input onChange="addPackageDate('${packageDetail[i].id}')" id="packageDateFrom-${packageDetail[i].id}" type="text" class="form-control datePicker packageDateList" required>
                                        </div -->
                                    </div>
                                </div>
                            `)
                        }

                    }

                    $('#totalResorPrice').text(`IDR ${formatRupiah(parseFloat(totalPrice))}`)
                    showUnavailableDates();
                } else {
                    alert(result.message)
                    console.log(result)
                }
            },
            error(xhr, status, error) {
                if(xhr.responseJSON){
                    console.log(xhr.responseJSON)
                }
            }
        })
    }

    function addPackageDate(packageId, packageItemId){
        for(var i=0; i<package_data.package.length; i++){
            for(var j= 0; j < package_data.package[i].packageItem.length; j++){
                if(package_data.package[i].id == packageId && package_data.package[i].packageItem[j].id == packageItemId){
                    checkUnavailableDatesPackage(`packageDateFrom-${packageId}-${packageItemId}`);
                    package_data.package[i].packageItem[j].package_date = dateFormatForDB($(`#packageDateFrom-${packageId}-${packageItemId}`).val())
                }
            }
        }
    }

    function checkout(){

        var dateData = ""
        if(package_data.room){
            if($('#roomDateFrom').val() == "" || $('#roomDateTo').val() == ""){
                // return alert("Invalid date")
                return Swal.fire(
                'Oops!',
                'Make sure your travel dates are selected.'
                )
            }

            package_data.room_date = {
                from : dateFormatForDB($('#roomDateFrom').val()),
                to : dateFormatForDB($('#roomDateTo').val())
            }

            dateData = dateFormatForDB($('#roomDateFrom').val())
        }

        if(package_data.package){
            for (let p_idx = 0; p_idx < package_data.package.length; p_idx++) {
                let package_id = package_data.package[i].id;

                let packageItem = package_data.package[i].packageItem;

                for(var j = 0; j < packageItem.length; j++){
                    if($('#packageDateFrom-'+package_id+'-'+packageItem[j].id).val() == ""){
                    // return alert("Invalid date")
                        return Swal.fire(
                            'Oops!',
                            'Make sure your travel dates are selected.'
                        )
                    }
                    // package_data.package[p_idx].packageItem[j].package_date = {
                    //     from : $('#packageDateFrom-'+package_id+'-'+packageItem[j].id).val()
                    // }

                    if(dateData == ""){
                        // console.log("kosong")
                        dateData = dateFormatForDB($('#packageDateFrom-'+package_id+'-'+packageItem[j].id).val())
                    }else{
                        // console.log("kosong")
                        if(new Date(dateData) - new Date(dateFormatForDB($('#packageDateFrom-'+package_id+'-'+packageItem[j].id).val())) > 0){
                            dateData = dateFormatForDB($('#packageDateFrom-'+package_id+'-'+packageItem[j].id).val())
                        }
                    }
                }
            }
        }

        // console.log('1',checkUnavailableDates())
        // console.log('2',checkAllPackageDates())
        if (checkUnavailableDates() && checkAllPackageDates()) {
            // $('#roomDateFrom, #roomDateTo, .packageDateList').prop('disabled',true);        
            // $('#checkoutButton').prop('disabled',true).html(`<span class="spinner-grow spinner-grow-sm" role="status" aria-hidden="true"></span>
            // Loading...`);
            $("#overlay-checkout").removeClass('d-none');
            $.ajax({
                url: `/api/transaction-resort/${id}/schedule`,
                method: `PUT`,
                data : {
                    begin_time : new Date(dateData),
                    package_data : package_data
                },
                success: function (result) {
                    console.log(result)
                    if (result.success) {
                        location.href = `/checkout-resort/${result.data.id}/extra`
                    } else {
                        alert(result.message)
                        console.log(result)
                        $("#overlay-checkout").addClass('d-none');
                        // $('#roomDateFrom, #roomDateTo, .packageDateList').prop('disabled',false);        
                        // $('#checkoutButton').prop('disabled',false).html(`Continue to Step 2`);
                    }
                },
                error(xhr, status, error) {
                    if(xhr.responseJSON){
                        console.log(xhr.responseJSON)
                    }
    
                    if (xhr.responseJSON.message == 'Token is required') {
                        $("#overlay-checkout").addClass('d-none');
                        loginRequired();
                    }
                }
            })
        
            
        }
    
    }

    function showUnavailableDates(){
        var info = '';
        const today = new Date();
        for (let i = 0; i < unDates.length; i++) {
            let t = new Date(unDates[i].date_end)
            console.log(today,' vs ', t)
            if (unDates[i].annually == "true") {
                let tgl_start = unDates[i].date_start;
                let tgl_end = unDates[i].date_end;
                unDatesAnnualStart.push(tgl_start);
                unDatesAnnualEnd.push(tgl_end);
                tgl_start = dateFormatMonth(tgl_start);
                tgl_end = dateFormatMonth(tgl_end);
                if (tgl_start == tgl_end) {
                    info += `<li> Every <b>${tgl_start.substr(0, tgl_start.length - 5)}</b> due to ${unDates[i].name}</li>`;
                }else{
                    info += `<li> Every <b>${tgl_start.substr(0, tgl_start.length - 5)} - ${tgl_end.substr(0, tgl_end.length - 5)}</b> due to ${unDates[i].name}</li>`;
                }
            }else{
                // console.log()
                if(today <= t){
                    let tgl_start = unDates[i].date_start;
                    let tgl_end = unDates[i].date_end;
                    unDatesOnceStart.push(tgl_start)
                    unDatesOnceEnd.push(tgl_end)
                    if (tgl_start == tgl_end) {
                        info += `<li> <b>${dateFormatMonth(unDates[i].date_start)}</b> due to ${unDates[i].name}</li>`;
                    }else{
                        info += `<li> <b>${dateFormatMonth(unDates[i].date_start)} - ${dateFormatMonth(unDates[i].date_end)}</b> due to ${unDates[i].name}</li>`;
                    }
                }
            }        
        }

        // console.log(unDatesAnnual)
        // console.log(unDatesOnce)

        if (info != '') {
            $(`#unavailableDatesInfo`).html("We're unavailable on these dates : <br/><ul>"+info+"</ul>");
        }else{
            $(`#unavailableDatesInfo`).html("Currently all dates are available");
        }
    }
</script>